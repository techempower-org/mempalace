*.egg-info/
dist/
build/
__pycache__/
*.pyc
.pytest_cache/
mempal.yaml
.a5c/
.claude/
.codex/
.codex

# Environment
.env
.env.*

# OS
.DS_Store
Thumbs.db

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Coverage
htmlcov/
.coverage
coverage.xml

# Virtual environments
.venv/
venv/
.claude-plugin/venv

# ChromaDB local data
*.sqlite3-journal

# Private / shared artifacts that should never land in the public fork
dashboards-bensig-*/

# Per-session scratch — workspace state, one-shot scripts, transient
# logs. Includes our recovery scripts, debug probes, HANDOFF.md.
# Belongs in ~/.claude/projects/-home-jp/scratch/ for compaction
# durability, not in the public repo.
scratch/

.envrc

# Local benchmark artifacts (large jsonl dumps, per-question stdout)
benchmarks/c_beta/*.jsonl
benchmarks/c_beta/*.stdout

# LongMemEval benchmark data (large — download per environment)
# See benchmarks/longmemeval_bench.py for the data shape.
benchmarks/data/
